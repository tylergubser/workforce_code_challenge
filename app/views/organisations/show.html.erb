<h1>Organisation</h1>
<p id="notice"><%= notice %></p>
<% Organisation.find(params[:id]) %>
<%= "#{@organisation.name}" %>

<%= link_to 'Edit', edit_organisation_path(@organisation) %> | <%= link_to 'Back', organisations_path %>

