
<h2>Organisation</h2>
<p id="notice"><%= notice %></p>
<% Organisation.find(params[:id]) %>
<%= "#{@organisation.name}" %>

<td><%= link_to 'View Shifts', shifts_path %></td> | <%= link_to 'Edit', edit_organisation_path(@organisation) %> | <%= link_to 'Leave', user_leave_organisation_path(current_user) %>

