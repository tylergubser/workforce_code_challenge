<h1>Adnat</h1>
<tr>
<td><% @user = User.find(session[:user_id]) %></td>
<td><%= "Logged in as #{@user.name} "%></td>
<td><%= link_to 'Logout', root_path %></td>
</tr>
<h1>Organisation</h1>
<p id="notice"><%= notice %></p>
<% Organisation.find(params[:id]) %>
<%= "#{@organisation.name}" %>

<td><%= link_to 'View Shifts', shifts_path %></td> | <%= link_to 'Edit', edit_organisation_path(@organisation) %> | <%= link_to 'Leave', organisations_path %>

